-- Auto-generated during release. Do not edit.
PeaversChangelogs = PeaversChangelogs or {}
PeaversChangelogs["PeaversSplits"] = {
    version = "0.1.0",
    entries = {
        { type = "feature", text = "Calls out each boss's split against the published pace, as the key runs" },
        { type = "feature", text = "Matched on your exact keystone level - never against a neighbouring one" },
        { type = "feature", text = "Says when a split lands inside the pool's usual range, so a delta can be told from noise" },
        { type = "feature", text = "Says what it is pacing against at the start, or that nothing is published yet" },
        { type = "feature", text = "Recovers a key already in progress after a reload" },
    }
}
